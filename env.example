# CueLABS™ Platform Environment Configuration
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local or any file containing real secrets to version control

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Airtable API Key (Public - accessible in client-side code)
# Get this from: https://airtable.com/account (under API section)
# This key allows the application to read/write data to your Airtable base
NEXT_PUBLIC_AIRTABLE_API_KEY=your_airtable_api_key_here

# Airtable Token (Alternative to API key)
# Personal access token for Airtable API
NEXT_PUBLIC_AIRTABLE_TOKEN=your_airtable_token_here

# Airtable Base ID (Public - accessible in client-side code)
# Get this from your Airtable base URL or API documentation
# Format: appXXXXXXXXXXXXXX (starts with 'app')
# Example: appAbc123DefGhi456
NEXT_PUBLIC_AIRTABLE_BASE_ID=your_airtable_base_id_here

# Additional Base ID (if using multiple bases)
NEXT_PUBLIC_BASE_ID=your_additional_base_id_here

# Table ID for specific Airtable table
# Format: tblXXXXXXXXXXXXXX (starts with 'tbl')
NEXT_PUBLIC_TABLE_ID=your_table_id_here

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================

# NextAuth.js Configuration
# The base URL of your application
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# NextAuth.js Secret
# Generate a random secret for JWT encryption
# You can generate one at: https://generate-secret.vercel.app/32
# Or use: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here

# GitHub OAuth Configuration
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Set Authorization callback URL to: {NEXTAUTH_URL}/api/auth/callback/github

# GitHub OAuth Client ID
# This is public and safe to expose in client-side code
GITHUB_CLIENT_ID=your_github_client_id_here

# GitHub OAuth Client Secret
# Keep this secret and never expose it in client-side code
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# =============================================================================
# ANALYTICS & MONITORING (OPTIONAL)
# =============================================================================

# Vercel Analytics ID
# Get this from your Vercel dashboard if you're using Vercel Analytics
# Leave empty if not using Vercel Analytics
VERCEL_ANALYTICS_ID=

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Node Environment
# Set to 'development' for local development
# Set to 'production' for production deployment
# Set to 'test' for running tests
NODE_ENV=development

# =============================================================================
# ADDITIONAL CONFIGURATION (OPTIONAL)
# =============================================================================

# Custom Domain (Production only)
# If you're using a custom domain in production, set it here
# CUSTOM_DOMAIN=yourdomain.com

# Debug Mode
# Set to 'true' to enable additional logging and debug information
# Only use in development - never in production
# DEBUG=false

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. AIRTABLE SETUP:
#    - Create an Airtable account at https://airtable.com
#    - Create a new base or use existing one
#    - Set up required tables (see docs/setup/airtable-setup.md)
#    - Get your API key or personal access token from https://airtable.com/account
#    - Get your Base ID from the Airtable API documentation
#    - Get your Table ID from the specific table you want to use

# 2. GITHUB OAUTH SETUP:
#    - Go to GitHub Settings > Developer settings > OAuth Apps
#    - Click "New OAuth App"
#    - Fill in application details:
#      * Application name: CueLABS™ Platform (or your preferred name)
#      * Homepage URL: http://localhost:3000 (for development)
#      * Authorization callback URL: http://localhost:3000/api/auth/callback/github
#    - Copy the Client ID and Client Secret

# 3. NEXTAUTH SECRET:
#    - Generate a random 32-character string
#    - You can use: https://generate-secret.vercel.app/32
#    - Or command line: openssl rand -base64 32

# 4. COPY AND RENAME:
#    - Copy this file to .env.local
#    - Fill in all the required values
#    - Never commit .env.local to version control

# =============================================================================
# TROUBLESHOOTING
# =============================================================================

# Common Issues:
# 1. "Invalid API key" - Check your NEXT_PUBLIC_AIRTABLE_API_KEY or NEXT_PUBLIC_AIRTABLE_TOKEN is correct
# 2. "Base not found" - Verify your NEXT_PUBLIC_AIRTABLE_BASE_ID format and permissions
# 3. "OAuth error" - Ensure GitHub OAuth callback URL matches exactly
# 4. "NextAuth error" - Make sure NEXTAUTH_SECRET is set and NEXTAUTH_URL is correct
# 5. "Table not found" - Verify your NEXT_PUBLIC_TABLE_ID is correct

# For more help, see:
# - docs/setup/airtable-setup.md
# - docs/setup/github-oauth-setup.md
# - docs/troubleshooting.md
